This is an experiment using Claude to create a complete assembler for Commodore 64.

Two versions exists here, one written in Python, the other one written in C.

To compile the C version, run ./m

To test run "hello.asm", run ./r hello

A reference manual is located under doc folder.

